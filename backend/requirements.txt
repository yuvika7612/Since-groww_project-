fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
pydantic==2.9.0
pydantic-settings==2.5.0
pandas>=2.2
numpy>=1.26
httpx==0.27.2
yfinance==0.2.44  # Yahoo API may reject this version; see README
# Only needed when redis_url is set. app/cache.py imports it lazily so a bare
# clone runs on the in-memory fallback without it installed.
redis==5.0.8
pytest==8.3.0

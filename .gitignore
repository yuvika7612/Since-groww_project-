__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
.env
backend/data/*.db
# WAL sidecars, created since db.py enables journal_mode=WAL
backend/data/*.db-shm
backend/data/*.db-wal
backend/data/*.jsonl
node_modules/
dist/
.DS_Store
.vscode/

# Working notes for the build, not part of the submission. Kept on disk
# because the agent reads them; the shipped README is written separately.
CLAUDE.md
BUILD_PLAN.md
SETUP.md
